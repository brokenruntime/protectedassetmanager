# protectedassetmanager
A secure Python library that encrypts folders into multiple protected fragments, which can only be decrypted and fully restored with the correct password and authentication token.
